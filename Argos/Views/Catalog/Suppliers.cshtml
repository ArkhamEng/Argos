@model Argos.ViewModels.Generic.PersonFilterViewModel<Supplier>
@using Argos.Models.Operative
@{
    ViewBag.Title = "Catálogo de Proveedores";
    ViewBag.Class = "fa fa-handshake-o";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@Html.Partial("_CatalogTitle")

<div class="panel panel-danger">
    <div class="panel-body">
        <div class="col-md-11">
            <div class="input-group ">
                @Html.TextBox("txtFtr", "", htmlAttributes: new
           {
               @class = "form-control",
               placeholder = "RFC",
               style = "width:30%"
           })
                @Html.TextBox("txtName", "", htmlAttributes: new
           {
               @class = "form-control",
               placeholder = "Clave o nombre del Proveedor",
               style = "width:70%"
           })
                <span class="input-group-btn">
                    <button class="btn btn-primary" type="button" id="btnSearch" onclick="SearchSuppliers()">
                        <span class="glyphicon glyphicon-search"></span>
                        <u>B</u>uscar
                    </button>
                    <button type="button" class="btn btn-success" id="btnCreate" onclick="BeginAddSupplier()">
                        <span class="glyphicon  glyphicon-file"></span>
                        <u>N</u>uevo
                    </button>
                </span>
            </div>
        </div>
        
        <div class="col-md-10" style="margin-top:10px">
            <div class="input-group">
                <span class="input-group-addon"><i class="glyphicon glyphicon-filter"></i></span>
                @Html.DropDownList("ddlStateId", Model.States, "Estado / Entidad", new { @class = "form-control" })
                <span class="input-group-addon"><i class="glyphicon glyphicon-filter"></i></span>
                @Html.DropDownList("ddlCityId", Model.Cities, "Ciudad / Municipio", new { @class = "form-control" })
                <span class="input-group-btn">
                    <button class="btn btn-info" type="button" id="btnClear" onclick="Clear()">
                        <span class="glyphicon glyphicon-refresh"></span>
                        <u>L</u>impiar
                    </button>
                </span>
            </div>
        </div>
    </div>
</div>

<div class="panel panel-default">
    <div id="divSupplierList" class="panel-body" style="min-height:300px">
        @Html.Partial("_SupplierList", Model.Entities)
    </div>
</div>

<script>
    $(document).ready(function ()
    {
        SetCascade('#ddlStateId', '#ddlCityId', '@Url.Action("GetCities","Configuration")');
    });

    function BeginAddSupplier()
    {
       ShowSupplierModal(SearchSupplierById, null, null);
    }

    function BeginUpdateSupplier(id)
    {
        ShowSupplierModal(SearchSupplierById, null, id);
    }


    function SearchSupplierById(id)
    {
        ExecuteAjax('@Url.Action("SearchSuppliers")', { id: id }, function (response)
        {
            $("#divSupplierList").html(response);
        });
    }

    function SearchSuppliers()
    {
        ShowLoading('static');

        var data = {
            ftr: $("#txtFtr").val(), name: $("#txtName").val(),
            stateId: $("#ddlStateId").val(), cityId: $("#ddlCityId").val()
        };

        ExecuteAjax('@Url.Action("SearchSuppliers")', data, function (response)
        {
            HideLoading(function ()
            {
                $("#divSupplierList").html(response);
            });
        });
    }

    function Clear()
    {
        window.location = window.location;
    }
</script>